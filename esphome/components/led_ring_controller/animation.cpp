#include "animation.h"

namespace esphome {
namespace led_ring_controller {
// Concrete Animation subclasses are added in issues 08 and 09.
}  // namespace led_ring_controller
}  // namespace esphome
